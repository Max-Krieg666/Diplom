<%= form_for(@discipline) do |f| %>
  <%= error_messages_for(@discipline) %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit t(:save) %>
  </div>
<% end %>
