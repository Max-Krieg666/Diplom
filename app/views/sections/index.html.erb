<h1>Все Sections</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Content</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @sections.each do |section| %>
      <tr>
        <td><%= section.title %></td>
        <td><%= section.content %></td>
        <td><%= link_to t(:show), section %></td>
        <td><%= link_to t(:edit), edit_section_path(section) %></td>
        <td><%= link_to t(:destroy), section, method: :delete, data: { confirm: t(:confirm) } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Создание Section', new_section_path %>
