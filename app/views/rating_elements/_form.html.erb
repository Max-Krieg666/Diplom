<%= form_for(@rating_element) do |f| %>
  <% if @rating_element.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rating_element.errors.count, "error") %> prohibited this rating_element from being saved:</h2>

      <ul>
      <% @rating_element.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :score %><br>
    <%= f.number_field :score %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
