<%= form_for(@student_rating_element) do |f| %>
  <%= error_messages_for(@student_rating_element) %>

  <div class="field">
    <%= f.label :value %><br>
    <%= f.number_field :value %>
  </div>
  <div class="actions">
    <%= f.submit t(:save) %>
  </div>
<% end %>
