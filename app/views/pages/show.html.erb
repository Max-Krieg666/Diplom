<p>
  <strong>Title:</strong>
  <%= @page.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @page.content %>
</p>

<%= link_to 'Редактировать', edit_page_path(@page) %> |
<%= link_to 'Назад', pages_path %>
