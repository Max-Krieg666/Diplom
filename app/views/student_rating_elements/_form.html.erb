<%= form_for(@student_rating_element) do |f| %>
  <% if @student_rating_element.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student_rating_element.errors.count, "error") %> prohibited this student_rating_element from being saved:</h2>

      <ul>
      <% @student_rating_element.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :value %><br>
    <%= f.number_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
